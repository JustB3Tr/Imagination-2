# Training stack for Imagination 2 (install PyTorch for your CUDA stack first — Colab usually has torch).
# Then:
#   pip install -r versions/1/training/requirements.txt

transformers>=4.38.0
accelerate>=0.27.0
peft>=0.10.0
trl>=0.8.0
datasets>=2.16.0
safetensors>=0.4.0
sentencepiece>=0.1.99
protobuf>=3.20
tqdm>=4.65.0
bitsandbytes>=0.43.0
wandb>=0.16.0
